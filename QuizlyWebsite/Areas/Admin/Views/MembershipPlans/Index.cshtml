@model IEnumerable<QuizlyWebsite.Models.TbMembershipPlan>

@{
    ViewData["Title"] = "Quản Lý Gói Thành Viên";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="flex justify-between items-center mb-8">
    <h2 class="text-3xl font-bold">Quản Lý Gói Thành Viên</h2>
    <a asp-action="Create" class="px-6 py-3 bg-primary text-white rounded-lg font-semibold hover:bg-primary-dark transition flex items-center gap-2">
        <span class="material-symbols-outlined">add_circle</span>
        Thêm Gói Thành Viên
    </a>
</div>

@if (TempData["Success"] != null)
{
    <div class="bg-success/10 border border-success/30 text-success rounded-lg p-4 mb-6">
        <p class="font-semibold">@TempData["Success"]</p>
    </div>
}

<div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 overflow-hidden">
    <div class="overflow-x-auto">
        <table class="w-full">
            <thead class="bg-gray-50 dark:bg-gray-700/50 border-b border-gray-200 dark:border-gray-700">
                <tr>
                    <th class="px-6 py-4 text-left text-sm font-semibold">ID</th>
                    <th class="px-6 py-4 text-left text-sm font-semibold">Tiêu Đề</th>
                    <th class="px-6 py-4 text-left text-sm font-semibold">Giá</th>
                    <th class="px-6 py-4 text-left text-sm font-semibold">Thời Hạn (Ngày)</th>
                    <th class="px-6 py-4 text-left text-sm font-semibold">Hành Động</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="border-b border-gray-200 dark:border-gray-700 hover:bg-gray-50 dark:hover:bg-gray-700/50 transition">
                        <td class="px-6 py-4 font-semibold text-[#0d141b] dark:text-slate-50">@item.Id</td>
                        <td class="px-6 py-4 font-semibold text-[#0d141b] dark:text-slate-50">@(item.Title ?? "N/A")</td>
                        <td class="px-6 py-4 text-[#4c739a] dark:text-slate-400">@(item.Price?.ToString("N0") ?? "0") VND</td>
                        <td class="px-6 py-4 text-[#4c739a] dark:text-slate-400">@(item.DurationDays ?? 0) ngày</td>
                        <td class="px-6 py-4">
                            <div class="flex gap-2">
                                <a asp-action="Details" asp-route-id="@item.Id" class="text-blue-500 hover:text-blue-700" title="Chi tiết">
                                    <span class="material-symbols-outlined text-sm">info</span>
                                </a>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="text-primary hover:text-primary-dark" title="Chỉnh sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="text-danger hover:text-red-700" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@if (!Model.Any())
{
    <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-12 text-center">
        <span class="material-symbols-outlined text-4xl text-gray-300 dark:text-gray-700 mb-4 block">card_membership</span>
        <p class="text-[#4c739a] dark:text-slate-400">Chưa có gói thành viên nào. <a asp-action="Create" class="text-primary hover:underline">Tạo gói thành viên mới</a></p>
    </div>
}

